const defaultsState = {
  currentUser: {name: 'Leonid'},
  isAuth: false
}

export default function userReducer(state = defaultsState, action) {
  switch (action.type) {

    default:
      return state
  }
}